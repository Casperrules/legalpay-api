[build]
builder = "NIXPACKS"
buildCommand = "mvn clean install -DskipTests && cp legalpay-api/target/*.jar app.jar"

[deploy]
startCommand = "java -jar app.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
SPRING_PROFILES_ACTIVE = "prod"
PORT = "8080"
